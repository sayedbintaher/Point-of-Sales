namespace PosAPI.Models.Common
{
    public static class AppConstants
    {
        public enum StatusId : byte
        {
            InActive = 0,
            Active = 1,
            Delete = 2
        }
    }
}
